version: "2"
services:
        backend:
                build: ./Back
                image: ubuntu
                ports:
                        - "80:5000"
                links:
                        - db
                        - db:docker-mysql
                environment:
                        DB_HOST: docker-mysql
        database:
                build: ./BD/
                image: mysql
                ports:
                        - "3306:3306"
                environment:
                        - MYSQL_ROOT_PASSWORD=root
                        - MYSQL_DATABASE=inscricao
        
        # frontend:
        #         build: ./Web2
        #         image: debian
        #         ports:
        #                - 80:80
        #         links:
        #                - backend



